func power_set(S) = 
    if #S == 0 then
        {{}}
    else
        let e = arb S
        let T = S \ {e}
        let P_T = power_set(T)
        
        P_T ∪ {t ∪ {e} | t ∈ P_T}

let S = power_set({0, 1, 2, 3})
println(S)
println("# = " + (#S))