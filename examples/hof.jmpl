func map(S, f) = {f(x) | x ∈ S}

func filter(S, f) = {x ∈ S | ¬f(x)}

func fold(S, b, f) = 
    let temp = b;
    for x ∈ S do
        temp := f(temp, x)
    temp

func add(x, y) = x + y

let S = {1 ... 10}
println(fold(S, 0, add))